backend:
name: github
repo: piiseli/oppia
branch: master

media_folder: media

collections:
- name: post
label: Post
folder: posts
create: true
fields:
  - {name: title, label: Title}
  - {name: body, label: Body, widget: markdown}
